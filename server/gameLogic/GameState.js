const gameState = {
    playerStats: {
        id: String,
        totalCoins: Number,
        roundCoins: Number,
    },
    totalPlayers,
    round: Number,
    activatedPerks: Array,
    timeLeft: Number,
    gridLayout: Array,
}

module.exports = {
    gameState
}