const gameState = {
    playerStats: {
        id: String,
        totalCoins: Number,
        roundCoins: Number,
    },
    totalPlayers: Number,
    gridLayout: Array,
    round: Number,
    activatedPerks: Array,
    timeLeft: Number,
    gridLayout: Array,
}

module.exports = {
    gameState
}